import { getProfile } from "./profile.tool";
import { getEducation } from "./education.tool";
import { getExperience } from "./experience.tool";
import { getSkills } from "./skills.tool";
import { getProjects, getProject, searchProjects } from "./projects.tool";
import { searchKnowledge } from "./knowledge.tool";

export const aiTools = {
  getProfile,
  getEducation,
  getExperience,
  getSkills,
  getProjects,
  getProject,
  searchProjects,
  searchKnowledge,
};

export {
  getProfile,
  getEducation,
  getExperience,
  getSkills,
  getProjects,
  getProject,
  searchProjects,
  searchKnowledge,
};
